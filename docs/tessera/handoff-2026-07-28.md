# Tessera → Tessera-on-Amber: Migration Handoff

**Written:** 2026-07-28, by the outgoing laptop/pipermorgan.ai session, at xian's request.
**Format:** follows the cohort's three-piece-package pattern, with HOST's 2026-07-25
revisions applied — claims are marked **[verified]** (this session watched it run)
or **[believed]** (reasonable inference, test before building on it); there is no
environment section for Amber (your provisioner, Pard, owns that — §4 lists the
questions to ask instead).

---

## §1 Who you are, in brief

Tessera, resident agent of Tectonic Globe (`Design-in-Product/globe`), working
ROADMAP.md with a standing autonomy mandate from xian ("no deadline, joy project,
we keep moving"). Working principles live at the top of `SESSION_LOG.md`
(Gall's Law; small reversible steps; verify before spending; leave a trail) —
they have earned their keep repeatedly **[verified]**. Daily logs in `logs/`,
cross-session narrative in `SESSION_LOG.md`, both maintained through 2026-07-28.

## §2 State of the work

- **Roadmap #1 (spin reveal): SHIPPED.** v7 released to globe.dinp.xyz on
  2026-07-14 — hero video, timeline card, OG metadata **[verified — site pushed,
  commits ae30794c/a57766b0]**.
- **Flat-v7 (projection morph): technique approved, production build NOT started.**
  xian approved the HD Mollweide morph ("very smooth action") **[verified]**.
  The lens-per-hold pitch — Rodinia sinusoidal → Gondwana south-polar azimuthal →
  Pangaea Mollweide → Present orthographic (flat map becomes the globe) — was
  delivered 2026-07-15 and **has no verdict yet** from xian.
- **Roadmap #2 (deep-time prequel): de-risked, design phase.** cao2024 loads by
  name via plate_model_manager and matches Merdith at the 1000 Ma handoff
  **[verified — see test_deeptime frames]**. The ensemble-superposition
  uncertainty treatment (xian's probability-cloud concept) was delivered
  2026-07-15 and **has no verdict yet**. Production design questions open:
  pacing across 1.8 Gyr, Nuna hold placement, per-plate vs whole-frame treatment.
- **Never done:** the letter to Janus/hub sharing the distributed-render pattern
  (was queued 2026-07-14, superseded by events — the network learned it from
  PM's migration anyway).

## §3 Load-bearing vs commodity

**Load-bearing (protect these):**
- `camera_path_spin_v8.json` — THE approved camera path (all four spins, Gondwana
  recentered). The v7 render came from it **[verified]**.
- `scripts/add_spin_reveal.py` — spin post-processor; `--target-hold` (pacing) and
  `--recenter` (hold framing) encode design decisions xian approved **[verified]**.
- `scripts/test_flat_morph.py` — the approved flat-v7 technique (per-pixel mesh,
  bilinear, projection morph + rotate-under) **[verified at HD]**.
- `scripts/test_ensemble_uncertainty.py` — the superposition treatment, including
  the DateLineWrapper fix **[verified at strip quality]**.
- Design knowledge not in code: (a) area-weighted centroids DEGENERATE for
  near-global landmass clusters — hand-frame those holds (2026-07-13 log);
  (b) uncertainty = ensemble superposition, NOT blur — xian's explicit concept;
  (c) the lens-arc pitch and its rationale (2026-07-15 log).
- `docs/research/deep-time-plate-models-2026-07-13.md` — prequel data decision.

**Commodity (regenerable, don't chase):** all test_*/check_* draft dirs,
verify PNGs, render logs. v7 frames exist on Amber at `~/globe-render/render_frames_v7/`
**[believed current — verify count 2738 before relying]** and on the laptop
(gitignored).

## §4 Questions for your provisioner (Pard), rather than my guesses

1. Where does the globe repo checkout live on Amber, and is it the worktree
   pattern or a plain clone? (This session worked in
   `.claude/worktrees/roadmap-review-planning-418a49`, merged to local main,
   pushed to origin — all landed as of this handoff **[verified]**.)
2. Is `~/globe-render/` (Blender 5.1.2, frames, v8 path, render_frames_v7) still
   intact from the July 14 distributed render? **[believed]**
3. What python env has gplately/pygplates/cartopy on Amber? (On the laptop it was
   `~/Development/atlas/.venv`; Amber never needed one — renders there used only
   Blender **[verified]**. The prequel work NEEDS gplately on whatever machine
   generates frames.)
4. ffmpeg on Amber? (Was absent 2026-07-14 **[verified then]**; assembly ran on
   the laptop. Either install it or keep assembly wherever ffmpeg lives.)
5. Git identity to commit under, per Janus's 2026-07-24 hygiene memo — Amber is
   a shared host.
6. Does the cross-pollination delivery + inter-agent mail reach the Amber checkout,
   or does mail still land in a laptop checkout somewhere? (This session's memos
   sat unseen for 11 days because delivery went to a checkout my worktree didn't
   track — see 2026-07-28 log. Don't repeat that.)

## §5 Conventions in force (all xian-ratified)

- All Blender renders on Amber, nohup-detached, never harness-tied (2026-07-14;
  the harness-tied laptop render died silently and cost 3 h **[verified]**).
  Post-migration this means renders are LOCAL to you — the ssh dance is over.
- `FRAME_START`/`FRAME_END` env vars + skip-existing make renders resumable and
  splittable **[verified]**.
- Every preview MP4 sent to xian: `-movflags +faststart` (2026-07-15 **[verified
  — a non-faststart file failed on xian's phone]**).
- Land work on local main as you go — Janus scans main, not branches (2026-07-14).
  And per the 2026-07-27 brief: when scanning sibling repos, read `origin/main`
  explicitly after fetching.
- Outgoing inter-agent mail goes in the RECIPIENT's repo `docs/mail/` (2026-07-13).
- Memory snapshot from the outgoing account: `docs/tessera/memory-snapshot-2026-07-28/`
  (contents current as of this handoff; the studio-render-convention file's
  "ssh studio" framing is the main thing your new situation obsoletes).

## §6 Self-assessment, honestly

What went well: the draft→judge→extend rhythm with xian (EEVEE drafts before
Cycles; concept strip before production); root-causing before acting (Low Power
Mode, moov atom, the 41° Gondwana offset); logging discipline after day one.

What to watch: (a) I twice lost work/context to session-infrastructure blind
spots (harness-tied render; mail delivered to a checkout I wasn't reading) —
the pattern is *assuming my window on the world is the world*. Check the seams.
(b) Two xian verdicts (ensemble strip, lens arc) have been outstanding since
7/15 — re-surface them gently rather than assuming silence means no.

Welcome to Amber. The picture emerges one verified tile at a time. 🌍

— Tessera (laptop/pipermorgan.ai era, 2026-06-26 → 2026-07-28)
